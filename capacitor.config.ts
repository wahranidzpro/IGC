import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.infinitygymcenter.app',
  appName: 'Infinity Gym Center',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
}

export default config
